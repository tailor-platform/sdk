{
  "name": "example",
  "private": true,
  "description": "Example for Tailor Platform SDK usage",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "generate": "tailor-sdk generate -c tailor.config.ts",
    "generate:watch": "tailor-sdk generate -c tailor.config.ts --watch",
    "apply": "tailor-sdk apply -c tailor.config.ts",
    "test": "pnpm test:generator",
    "test:all": "pnpm test:generator:prepare && vitest",
    "test:generator": "pnpm test:generator:prepare && vitest --project generator",
    "test:generator:prepare": "tsx tests/scripts/generate_files.ts",
    "test:generator:update-expects": "tsx tests/scripts/generate_files.ts expected",
    "test:e2e": "vitest --project e2e",
    "migration:e2e": "tsx tests/scripts/migration_e2e.ts",
    "seed:validate": "node ./seed/exec.mjs validate",
    "seed:truncate": "tailor-sdk tailordb truncate -a",
    "seed": "node ./seed/exec.mjs",
    "analyze:bundle": "tsx tests/scripts/analyze_minified_size.ts",
    "lint": "oxlint . && eslint --cache .",
    "lint:fix": "oxlint --fix . && eslint --cache . --fix --quiet",
    "typecheck": "pnpm generate && tsc --noEmit",
    "typecheck:go": "tsgo"
  },
  "dependencies": {
    "@tailor-platform/sdk": "workspace:^",
    "@tailor-platform/tailor-proto": "workspace:^",
    "date-fns": "4.1.0"
  },
  "devDependencies": {
    "@connectrpc/connect": "2.1.1",
    "@connectrpc/connect-node": "2.1.1",
    "@types/node": "24.12.2",
    "@typescript/native-preview": "7.0.0-dev.20260406.1",
    "eslint-plugin-oxlint": "1.58.0",
    "graphql": "16.13.2",
    "graphql-request": "7.4.0",
    "oxfmt": "0.44.0",
    "oxlint": "1.59.0",
    "tsx": "4.21.0",
    "typescript": "5.9.3",
    "vitest": "4.1.2"
  },
  "engines": {
    "node": ">= 22.14.0"
  }
}
