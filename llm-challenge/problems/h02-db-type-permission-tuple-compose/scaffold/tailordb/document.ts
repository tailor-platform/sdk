// Define and export `document` as a TailorDB type named "Document" with two
// fields (title, ownerId) and a record-level permission whose `update` action
// composes two AND-ed conditions in a single policy entry.
// See problem.md for the full requirements.
