// Define and export `subscription` as a TailorDB type named "Subscription" with
// two fields (plan, price) and TYPE-LEVEL validators via `.validate({...})`.
// See problem.md for the full requirements.
