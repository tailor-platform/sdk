// Define and export `order` as a TailorDB type named "Order" with `status`
// (string) and `createdAt` (datetime) fields, then attach a non-unique
// composite index over ["status", "createdAt"].
// See problem.md for the full requirements.
