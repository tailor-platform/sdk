// TODO: Define and export the `member` TailorDB type per problem.md.
// The model must be a named export with a UUID id, a UUID
// organizationId carrying a many-to-one relation to the Organization
// model, a required email, an array-of-strings roles field, and a
// composite index on (organizationId, email).
export const member = null as unknown;
