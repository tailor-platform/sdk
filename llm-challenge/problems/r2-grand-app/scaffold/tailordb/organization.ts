// TODO: Define and export the `organization` TailorDB type per problem.md.
// The model must be a named export, declare its UUID id, a required+unique
// name, a custom-validated slug, a tier string field, a type-level update
// hook that lowercases the slug, and a role-gated permission rule.
export const organization = null as unknown;
