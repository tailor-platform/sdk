{
  "id": "h10-config-multi-idp-cross-wire",
  "title": "Wire defineAuth.idProvider to the correct IdP across two defineIdp definitions",
  "designNote": "missing_namespace",
  "sdkSurface": "config",
  "contextProfiles": ["code-only", "code-and-docs", "bare-types"],
  "canonicalSymbols": ["defineIdp", "defineAuth", ".provider"],
  "biasAttractors": ["defineIdProvider", "defineProvider", "selectIdp", "useIdp", "idpProvider"],
  "expectedCallShapes": {
    "defineIdp": {
      "positionalArity": 2,
      "configArgIndex": 1,
      "configKeys": ["clients"]
    },
    "defineAuth": {
      "positionalArity": 2,
      "configArgIndex": 1,
      "configKeys": ["idProvider"]
    }
  }
}
