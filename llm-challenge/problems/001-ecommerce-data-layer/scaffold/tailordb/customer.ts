import { db } from "@tailor-platform/sdk";

// TODO: Define the Customer model with:
// - name (string, required)
// - email (string, required, validated to contain "@")
// - phone (string, optional)
// - address (nested object: street, city, state, zipCode)
// - timestamps
