// Define and export `product` as a TailorDB type named "Product" with a single
// `price` float field, then attach a type-level validate rule that rejects
// negative prices with the message "price must be >= 0".
// See problem.md for the full requirements.
