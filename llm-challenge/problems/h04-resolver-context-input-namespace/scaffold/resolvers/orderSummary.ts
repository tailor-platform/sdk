// Default-export a query resolver named "orderSummary" whose body accepts a
// single context argument and returns { orderId: input.orderId, viewerId:
// user.id }. See problem.md for the full requirements.
