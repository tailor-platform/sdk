Use the local `tailor` binary available after installing dependencies.

Start by using the CLI help and error output to discover how to perform a simple project maintenance action. Then run the corrected command and leave a short note in the workspace describing what you ran and why.

Choose an action that can complete locally without Tailor cloud credentials, external services, or additional npm packages beyond the workspace dependencies.

Do not rely on a globally installed SDK.
