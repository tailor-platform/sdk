// Define and export `document` as a TailorDB type named "Document" with three
// fields (title, slug, version) and attach type-level update hooks that handle
// null input by substituting a field-specific fallback.
// See problem.md for the full requirements.
