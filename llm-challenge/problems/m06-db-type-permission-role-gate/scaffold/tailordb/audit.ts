// Define and export `audit` as a TailorDB type named "Audit" with a single
// `message` string field, then attach a record-level permission gating
// create/read to logged-in users and update/delete to role=ADMIN.
// See problem.md for the full requirements.
