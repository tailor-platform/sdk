// Implement a workflow that uses `getDB("tailordb")` to sum Invoice.amount
// grouped by accountId. Named export the job as `sumInvoicesByAccount` and
// default-export the surrounding workflow. See problem.md for the full
// requirements.
