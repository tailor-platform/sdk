// Default-export `createExecutor` named "cancel-audit" that uses
// resolverExecutedTrigger({ resolver: cancelOrder }) and whose function body
// calls recordAudit({ source: "resolver", reference: args.resolverName }).
// See problem.md for the full requirements.
