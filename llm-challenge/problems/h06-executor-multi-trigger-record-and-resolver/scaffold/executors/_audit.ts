// Export a named `recordAudit({ source, reference })` helper that returns
// { entry: `<source>:<reference>` }. Both executor files must import this
// helper by relative path so the audit format stays uniform.
// See problem.md for the full requirements.
