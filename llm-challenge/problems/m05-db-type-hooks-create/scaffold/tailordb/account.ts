// Define and export `account` as a TailorDB type named "Account" with a single
// `slug` string field, then attach a type-level create hook that returns the
// lowercase version of the supplied slug.
// See problem.md for the full requirements.
