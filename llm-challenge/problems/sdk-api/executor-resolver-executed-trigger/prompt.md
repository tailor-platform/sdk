Create a minimal Tailor SDK project with one resolver-style operation and one executor-style handler.

The handler should run when the operation is executed and should record a small audit payload that includes the operation name and a timestamp. A reviewer should be able to inspect the connection between the operation execution and the handler.

Keep the implementation small and leave the finished project files in this workspace.
