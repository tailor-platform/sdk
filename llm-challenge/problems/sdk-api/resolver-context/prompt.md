Create a minimal Tailor SDK project with one resolver-style operation.

The operation should return a structured summary of the caller and request context available at runtime, including a caller identifier when present and a small environment-derived value. Keep the operation deterministic enough for a reviewer to understand without contacting external services.

Keep the implementation small and leave the finished project files in this workspace.
