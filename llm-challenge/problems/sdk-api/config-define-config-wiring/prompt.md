Create a minimal Tailor SDK project configuration for a small operations application.

Wire together one database area, one resolver-style operation, and one workflow so the project has a coherent top-level configuration. Use clear names that make it obvious how the pieces belong to the same application.

Keep the implementation small and leave the finished project files in this workspace.
