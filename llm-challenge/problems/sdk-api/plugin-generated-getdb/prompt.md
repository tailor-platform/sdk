Create a minimal Tailor SDK project with a local plugin that generates a database helper.

The generated helper should give application code a concise way to get a database handle for the project. Include one small usage site so a reviewer can see how the helper is intended to be consumed.

Keep the implementation small and leave the finished project files in this workspace.
