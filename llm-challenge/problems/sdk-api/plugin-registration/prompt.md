Create a minimal Tailor SDK project that registers a local plugin.

The plugin should add a small generated artifact or configuration contribution that a reviewer can identify after reading the source. Keep the plugin local to the project and wire it into the main project configuration.

Keep the implementation small and leave the finished project files in this workspace.
