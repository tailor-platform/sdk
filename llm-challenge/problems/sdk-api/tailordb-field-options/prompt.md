Create a minimal Tailor SDK project that models customer accounts in TailorDB.

The project should include a customer record with a stable identifier, contact email, display name, account tier, and signup time. Apply the appropriate field-level behavior so a human reviewer can see which values are required, which values must be unique, and which values have a constrained set of choices.

Keep the implementation small and leave the finished project files in this workspace.
