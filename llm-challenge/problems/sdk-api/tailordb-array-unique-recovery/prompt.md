Complete the minimal Tailor SDK project for creator profiles in TailorDB.

The workspace already contains starter files, but the model is not finished and may need correction before it typechecks. Each profile should keep a public handle that cannot be duplicated, a display name, an ordinary list of topical tags, an avatar label stored as regular profile data, and an uploaded avatar image. Keep the tag list as ordinary repeatable profile data; do not make the list itself a unique lookup key.

Keep the implementation small and leave the finished project files in this workspace.
