Create a minimal Tailor SDK project with one resolver-style operation for an inventory dashboard.

The operation should return a nested result containing a summary object and a list of item rows. The result shape should be explicit enough that another developer can use it from generated types without guessing field names.

Keep the implementation small and leave the finished project files in this workspace.
