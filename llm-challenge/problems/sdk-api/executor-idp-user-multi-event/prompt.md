Create a minimal Tailor SDK project that reacts to identity user lifecycle activity.

One executor-style handler should be connected to more than one user event from the identity provider area, such as user creation and user update activity. The handler can write a compact audit message or keep the implementation self-contained.

Keep the implementation small and leave the finished project files in this workspace.
