{
  "name": "@examples/basic",
  "version": "1.0.0",
  "description": "Basic example for Tailor SDK usage",
  "type": "module",
  "scripts": {
    "gen": "tailor-sdk generate -c tailor.config.ts",
    "gen:watch": "tailor-sdk generate -c tailor.config.ts --watch",
    "apply": "tailor-sdk apply -c tailor.config.ts",
    "test": "npm run test:prepare && vitest run",
    "test:prepare": "tsx tests/scripts/generate_files.ts actual",
    "test:update-expects": "tsx tests/scripts/generate_files.ts",
    "lint": "eslint --cache .",
    "lint:fix": "eslint --cache . --fix --quiet",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@tailor-platform/tailor-sdk": "workspace:^",
    "date-fns": "^4.1.0",
    "kysely": "0.27.6",
    "sqlstring": "^2.3.3"
  },
  "devDependencies": {
    "@types/node": "^22.15.3",
    "@types/sqlstring": "^2.3.2",
    "dir-compare": "^4.2.0",
    "tsx": "^4.19.2",
    "typescript": "^5.8.3",
    "vitest": "^3.1.4",
    "vitest-mock-extended": "^2.0.2"
  },
  "engines": {
    "node": ">= 22.14.0"
  }
}
