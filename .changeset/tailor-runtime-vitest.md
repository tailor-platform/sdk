---
"@tailor-platform/sdk": minor
---

Add `@tailor-platform/sdk/vitest` with tailor-runtime environment plugin for runtime emulation. Blocks `node:*` imports, auto-injects platform API mocks (tailordbMock, workflowMock), and migrates create-sdk templates to use the new environment.
