---
"@tailor-platform/sdk": minor
---

Convert a single-value field into an array with `tailordb migration generate --expand-contract`. The generated migration pair stores each existing value as a one-element array and needs no edits; array-to-single-value changes remain manual.
