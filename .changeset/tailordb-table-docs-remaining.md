---
"@tailor-platform/sdk": patch
"@tailor-platform/create-sdk": patch
---

The remaining published docs that named a `db.table()` definition a type now say table: the migration guide's clone and rebaseline sections, the testing guide's executor trigger description, and the create-sdk `tailordb` template README's feature list (`field-level and table-level` validations).
