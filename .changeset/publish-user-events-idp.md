---
"@tailor-platform/sdk": patch
---

Add `publishUserEvents` option for IdP configuration

When enabled, user lifecycle events (created, updated, deleted) are published to the dataplane event topic.
