---
"@tailor-platform/sdk": patch
---

Report a stable `error.code` with recovery guidance for user-actionable CLI command failures (invalid options, missing resources, invalid configuration, unmet preconditions) under `--json`, instead of `UNEXPECTED_ERROR`.
