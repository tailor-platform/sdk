---
"@tailor-platform/sdk": patch
---

Run array field custom validators once against the complete array, skip custom validation when built-in type validation fails, and reject cloning validated fields between scalar and array shapes.
