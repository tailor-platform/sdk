---
"@tailor-platform/sdk": minor
---

Show when each personal access token was created and last used in `tailor user pat list`, so unused tokens are easy to spot. Tokens that have never authenticated report `never`.
