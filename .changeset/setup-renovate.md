---
"@tailor-platform/sdk": minor
---

Add `tailor setup renovate` to generate a Renovate config that extends Tailor's shared preset without overwriting existing or customized configuration, and allow `tailor setup delete` to remove it.
