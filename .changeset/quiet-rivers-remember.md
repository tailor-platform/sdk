---
"@tailor-platform/sdk": patch
---

Preserve field hooks and boolean validator behavior when replaying migration snapshots and diffs generated by older SDKs, including existing field values during partial updates, nested fields, and the first validation error. Avoid false script-drift errors when deploying existing histories and preserve validation metadata when rebaselining.
