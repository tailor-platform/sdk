---
"@tailor-platform/sdk": patch
---

Preserve field hooks and boolean validator behavior when replaying migration snapshots and diffs generated by older SDKs, including existing field values during partial updates, nested fields, and the first validation error. Nested hooks retain omitted parents and respect explicit nulls and replacement objects or arrays, and nested validators read sibling fields from their own containing object or array element rather than the top-level record. Avoid false script-drift errors when deploying existing histories and preserve validation metadata when rebaselining.
