---
"@tailor-platform/sdk": minor
---

Add `secret import` command for bulk importing secrets from .env files into a vault. Supports file input (`-f .env`) and stdin (`-f -`), dry-run preview mode, and `--prefix` option to filter and strip key name prefixes.
