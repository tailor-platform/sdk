---
"@tailor-platform/create-sdk": patch
---

List every template `--template` accepts in the create-sdk README. The table named a `testing` template that no longer exists and left out `tailordb`, `resolver`, `workflow`, `executor`, `static-web-site` and `generators`.
