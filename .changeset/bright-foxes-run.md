---
"@tailor-platform/sdk": minor
---

Add `function test-run` CLI command to run functions on the Tailor Platform server without deploying. Supports auto-detection of resolver, executor, workflow job, and plain function types. Bundles the function using rolldown and executes via TestExecScript API.
