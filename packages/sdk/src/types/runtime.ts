// Re-exports from configure layer (needed because parser/plugin cannot import from configure)
export type { TailorActor, TailorActorType } from "@/configure/types/actor";
export type { TailorEnv } from "@/configure/types/env";
