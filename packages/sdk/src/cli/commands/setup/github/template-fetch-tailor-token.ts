/**
 * The fetch-tailor-token composite action YAML.
 *
 * Loaded from a .yml file so Renovate can detect and update GitHub Actions versions.
 */
import fetchTailorTokenYaml from "./fetch-tailor-token.action.yml";

export { fetchTailorTokenYaml };
