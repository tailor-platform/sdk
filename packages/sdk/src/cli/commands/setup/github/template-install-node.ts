/**
 * The install-node composite action YAML.
 * Targets pnpm projects -- the SDK scaffolds with pnpm and generated projects use pnpm.
 *
 * Loaded from a .yml file so Renovate can detect and update GitHub Actions versions.
 */
import installNodeYaml from "./install-node.action.yml";

export { installNodeYaml };
