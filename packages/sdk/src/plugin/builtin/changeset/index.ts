/**
 * Changeset plugin module.
 *
 * Provides the changeset plugin for approval workflow management.
 */

// Re-export plugin
export { default } from "./plugin";
export type { GeneratedTypeKind } from "./plugin";
