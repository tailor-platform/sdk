name: "@tailor-platform/apply-to-deploy"
version: "1.0.0"
description: "Rewrite `tailor-sdk apply` invocations to the canonical v2 `tailor-sdk deploy` command"
engine: jssg
language: text
since: "1.0.0"
until: "2.0.0"
