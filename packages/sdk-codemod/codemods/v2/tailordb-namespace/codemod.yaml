name: "@tailor-platform/tailordb-namespace"
version: "1.0.0"
description: "Rename the removed capital-cased `Tailordb` ambient namespace to lowercase `tailordb` (e.g. `Tailordb.QueryResult` → `tailordb.QueryResult`)"
engine: jssg
language: typescript
since: "1.0.0"
until: "2.0.0"
