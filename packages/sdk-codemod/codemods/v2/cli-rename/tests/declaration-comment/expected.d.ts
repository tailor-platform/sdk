/**
 * Plain prose mentions tailor-sdk crash-report --machineuser.
 * Inspect crash reports with `tailor-sdk crashreport list --machine-user ci`.
 * Example:
 * ```sh
 * tailor-sdk crashreport list --machine-user ci
 * ```
 */
export {};
