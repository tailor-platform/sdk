/**
 * Plain prose mentions tailor-sdk crash-report --machineuser.
 * Inspect crash reports with `tailor-sdk crash-report list --machineuser ci`.
 * Example:
 * ```sh
 * tailor-sdk crash-report list --machineuser ci
 * ```
 */
export {};
