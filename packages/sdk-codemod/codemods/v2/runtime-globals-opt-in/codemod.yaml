name: "@tailor-platform/runtime-globals-opt-in"
version: "1.0.0"
description: "Add the explicit runtime globals import for files that still use ambient `tailor.*` or `tailordb.*` references"
engine: jssg
language: typescript
since: "1.0.0"
until: "2.0.0"
