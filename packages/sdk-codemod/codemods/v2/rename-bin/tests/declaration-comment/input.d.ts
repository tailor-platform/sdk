/**
 * Run `tailor-sdk deploy` after editing the app config.
 * Regenerate types with `tailor-sdk generate`.
 * Keep the generated output directory `.tailor-sdk/` in place until v2 setup runs.
 */
export {};
