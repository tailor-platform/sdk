/**
 * Run `tailor deploy` after editing the app config.
 * Regenerate types with `tailor generate`.
 * Install a pinned CLI with `npx --package @tailor-platform/sdk tailor login`.
 * Keep the generated output directory `.tailor-sdk/` in place until v2 setup runs.
 */
export {};
