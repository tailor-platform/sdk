const siteName = "portal";
const setup = `pnpm tailor-sdk staticwebsite deploy --name ${siteName}`;
const deploy = "tailor-sdk deploy";
const dynamicCommand = `tailor-sdk ${subcommand}`;
const dynamicCommandAfterSeparator = `cd app && tailor-sdk ${subcommand}`;
const dynamicCommandAfterEnv = `env FOO=bar tailor-sdk ${subcommand}`;
const dynamicCommandAfterAssignment = `TAILOR=1 tailor-sdk ${subcommand}`;
const dynamicPnpmCommand = `pnpm tailor-sdk ${subcommand}`;
const dynamicPnpmExecCommand = `pnpm exec tailor-sdk ${subcommand}`;
const dynamicCommandWithFlags = `tailor-sdk ${flags} deploy`;
const dynamicCommandWithConditionalFlags = `tailor-sdk ${useJson ? "--json" : ""} deploy`;
const profileValue = "tailor-sdk --profile tailor-sdk deploy";
const shortProfileValue = "tailor-sdk -p tailor-sdk deploy";
const latest = "npx tailor-sdk@latest login";
const cacheRunner = "npx --cache .npm tailor-sdk login";
const registryRunner = "npx --registry=https://npm.example/tailor-sdk tailor-sdk login";
const pnpmOptionRunner = "pnpm --dir . dlx tailor-sdk login";
const inlineBooleanFlagRunner = "npx --yes=true tailor-sdk login";
const packageOptionRunner = "npx --package=tailor-sdk tailor-sdk login";
const separatePackageOptionRunner = "npx --package tailor-sdk tailor-sdk login";
const separatePackageOptionTemplate = `npx --package tailor-sdk tailor-sdk login`;
const quotedPackageOptionRunner = 'npx --package="tailor-sdk" tailor-sdk login';
const dynamicPackageOptionRunner = `npx --package=${useLocal ? "tailor-sdk" : pkg} tailor-sdk login`;
const dynamicSeparatePackageOptionRunner = `npx ${useLocal ? "--package" : ""} ${useLocal ? "tailor-sdk" : pkg} tailor-sdk login`;
const dynamicPackageOptionCommand = `npx --package tailor-sdk tailor-sdk ${subcommand}`;
const dynamicInlinePackageOptionCommand = `npx --package=${pkg} tailor-sdk ${subcommand}`;
const dynamicFlagStaticTemplate = `npx ${yes ? "-y" : ""} tailor-sdk login`;
const dynamicFlagConditionTemplate = `npx ${mode === "prod" ? "-y" : "--yes"} tailor-sdk login`;
const dynamicDlxValueFlagTemplate = `pnpm ${use ? "--filter" : ""} app dlx tailor-sdk login`;
const dynamicDlxInlineValueFlagTemplate = `pnpm ${use ? "--filter=tailor-sdk" : ""} dlx tailor-sdk login`;
const packageConditionTemplate = `npx ${pkg === "tailor-sdk" ? pkg : "tailor-sdk"} login`;
const quotedCacheTemplate = `npx --cache 'tailor-sdk/${cache}' tailor-sdk login`;
const quotedRegistryTemplate = `npx --registry "https://npm.example/tailor-sdk/${scope}" tailor-sdk login`;
const cacheValue = "npx --cache tailor-sdk some-package";
const outputDir = ".tailor-sdk/";
const scaffold = "create-tailor-sdk";
const skills = "tailor-sdk-skills install";
const proseTemplate = `package tailor-sdk ${version}`;
