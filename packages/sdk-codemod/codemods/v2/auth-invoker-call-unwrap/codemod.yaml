name: "@tailor-platform/auth-invoker-call-unwrap"
version: 0.1.0
description: 'Replace auth.invoker("name") with the bare string "name" while preserving the authInvoker option key'
language: typescript
