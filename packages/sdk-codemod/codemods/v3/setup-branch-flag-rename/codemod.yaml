name: "@tailor-platform/setup-branch-flag-rename"
version: "1.0.0"
description: "Rename the `--branch` option of `tailor setup branch` invocations to `--trigger-branch`"
engine: jssg
language: text
since: "1.72.0"
until: "3.0.0"
