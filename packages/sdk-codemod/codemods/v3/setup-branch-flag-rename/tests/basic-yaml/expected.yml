name: regenerate
jobs:
  setup:
    runs-on: ubuntu-latest
    steps:
      - run: tailor setup branch --name my-app-stg --target main
      - run: |
          tailor setup branch \
            --name my-app \
            --target main
      - run: tailor setup tag --name my-app-prod --branch main
