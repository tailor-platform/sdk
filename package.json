{
  "name": "tailor-dev-kit",
  "private": true,
  "description": "Tailor Development Kit - Workspace Root",
  "keywords": [],
  "license": "MIT",
  "author": "",
  "type": "module",
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm run build && pnpm -r run test",
    "dev": "pnpm -r run dev",
    "generate": "pnpm run build && pnpm -r run generate",
    "deploy": "pnpm run build && pnpm -r run deploy",
    "agent:rules:update": "node scripts/sync-agent-rules.mjs --write",
    "agent:rules:check": "node scripts/sync-agent-rules.mjs --check",
    "check": "pnpm run build && pnpm --filter @tailor-platform/sdk --filter example run generate && pnpm run format && pnpm run \"/^check:/\"",
    "check:agent-rules": "pnpm run agent:rules:check",
    "check:lint": "pnpm -r run lint:fix",
    "check:typecheck": "pnpm -r run typecheck:go",
    "check:knip": "pnpm -r run knip",
    "check:publint": "pnpm -r run publint",
    "check:jsdoc": "pnpm -r run check:public-api-jsdoc",
    "lint": "pnpm run build && pnpm -r run lint",
    "lint:fix": "pnpm run build && pnpm -r run lint:fix",
    "knip": "pnpm run build && pnpm -r run knip",
    "publint": "pnpm run build && pnpm -r run publint",
    "typecheck:go": "pnpm run build && pnpm --filter @tailor-platform/sdk --filter example run generate && pnpm -r run typecheck:go",
    "format": "oxfmt --write .",
    "format:check": "oxfmt --check .",
    "typecheck": "pnpm run generate && pnpm -r run typecheck",
    "typecheck:root": "tsc --noEmit",
    "perf:typecheck": "pnpm --filter example perf:typecheck"
  },
  "devDependencies": {
    "@changesets/changelog-github": "0.7.0",
    "@changesets/cli": "2.31.0",
    "@tailor-platform/sdk": "workspace:^",
    "@types/node": "24.12.4",
    "knip": "6.15.0",
    "lefthook": "2.1.9",
    "oxfmt": "0.53.0",
    "pkg-pr-new": "0.0.75",
    "publint": "0.3.21",
    "tsx": "4.22.4",
    "typescript": "5.9.3"
  },
  "engines": {
    "node": ">= 22.14.0"
  },
  "packageManager": "pnpm@11.5.1"
}
