name: Docs Implementation Consistency Check

on:
  pull_request:
    types: [ready_for_review, labeled]
    paths:
      - "packages/sdk/src/**"
      - "packages/sdk/docs/**"
      - "example/**"
      - "AGENTS.md"
      - "CLAUDE.md"

permissions:
  contents: read
  pull-requests: write
  id-token: write

jobs:
  docs-consistency:
    if: >
      github.event.action == 'ready_for_review'
      || (github.event.action == 'labeled' && github.event.label.name == 'docs-check')
    runs-on: ubuntu-slim
    timeout-minutes: 10
    env:
      PR_NUMBER: ${{ github.event.number }}
      REVIEW_BODY_FILE: ${{ github.workspace }}/docs-consistency-review.md
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
        with:
          persist-credentials: false

      - name: Check docs-implementation consistency
        uses: anthropics/claude-code-action@11ba60486e4aec9ddfeafcf4bb3f00b028ac2c16 # v1.0.142
        with:
          anthropic_api_key: ${{ secrets.ANTHROPIC_API_KEY }}
          prompt: |
            # Documentation vs Implementation Consistency Check

            Analyze the PR changes and verify that documentation stays consistent with the implementation.

            ## Target Documentation

            - `packages/sdk/docs/cli-reference.md` - CLI reference
            - `packages/sdk/docs/cli/*.md` - CLI subcommand docs
            - `packages/sdk/docs/services/*.md` - Service documentation
            - `packages/sdk/docs/configuration.md` - Configuration reference
            - `AGENTS.md` - Developer guide and code patterns
            - `CLAUDE.md` - Compatibility adapter for agent instructions
            - `example/` - Working example code

            ## Investigation Steps

            ### 1. Retrieve PR Changes

            ```bash
            gh pr view ${{ env.PR_NUMBER }} --json title,body,files
            gh pr diff ${{ env.PR_NUMBER }}
            ```

            ### 2. Classify Changes

            Determine which categories the PR changes fall into:

            - **CLI commands/options**: Added, changed, or removed commands or flags
            - **Public API**: New or modified exported functions, types, or interfaces
            - **Configuration**: Changes to config file schema (`defineConfig`, `defineAuth`, etc.)
            - **Executors/Resolvers/Workflows**: Changes to creation APIs or trigger types
            - **Example code**: Changes that should reflect current best practices

            ### 3. Cross-Reference Documentation

            For each changed area, check the corresponding documentation:

            #### 3.1 CLI Changes
            - Read `packages/sdk/docs/cli-reference.md` and relevant `packages/sdk/docs/cli/*.md` files
            - Verify all CLI commands, options, and their descriptions match the implementation
            - Compare source code command definitions with docs

            #### 3.2 API / Config Changes
            - Read `packages/sdk/docs/services/*.md` and `packages/sdk/docs/configuration.md`
            - Verify documented function signatures, parameters, and return types match the implementation
            - Check if new exports are documented

            #### 3.3 Pattern / Example Changes
            - Read relevant files in `example/` directory
            - Verify examples use current API patterns (not deprecated ones)
            - Check `AGENTS.md` "Code Patterns" and "Non-obvious Rules and Gotchas" sections

            #### 3.4 Reverse Check (docs changed but implementation not updated)
            - If documentation was changed, verify the implementation matches
            - Look for docs describing features that don't exist yet or have been removed

            ### 4. Write the Review Report

            Compile your findings into one of the markdown formats below and
            **write it to the file `${{ env.REVIEW_BODY_FILE }}` using the Write
            tool**.

            Do **not** post a review yourself — you have no tools to do so. A
            later workflow step reads this file, neutralizes any `@mentions`, and
            posts (or updates) the PR review for you. Your only deliverable is
            the file.

            **If inconsistencies are found**, write a review summary in this format:

            ```markdown
            ## 📖 Docs Consistency Check

            ### ⚠️ Inconsistencies Found

            | File | Issue | Suggested Fix |
            |------|-------|---------------|
            | `path/to/doc.md` | Description of mismatch | What should be updated |

            ### Details

            (For each inconsistency, explain what the implementation does vs what the documentation says,
            with references to specific file paths and line numbers.)

            ### Recommended Actions
            - ...

            > Re-run this check by adding the `docs-check` label to the PR.

            ---
            <!-- docs-consistency-check -->
            ```

            **If no inconsistencies are found**, post a review summary in this format:

            ```markdown
            ## 📖 Docs Consistency Check

            ✅ No inconsistencies found between documentation and implementation.

            **Checked areas:**
            - (list what was checked)

            > Re-run this check by adding the `docs-check` label to the PR.

            ---
            <!-- docs-consistency-check -->
            ```

            Always keep the `<!-- docs-consistency-check -->` marker at the end
            so the post step can de-duplicate the review.

            ## Important Notes
            - The PR branch is already checked out
            - Do not produce a conversational response; your output is the file
              `${{ env.REVIEW_BODY_FILE }}`
            - Be specific: include file paths and line numbers for every inconsistency
            - Only flag real inconsistencies, not stylistic differences
            - If unsure whether something is an inconsistency, mention it with a note that it may be intentional

          claude_args: |
            --model claude-sonnet-4-5-20250929
            --allowedTools "Bash(gh pr view:*),Bash(gh pr diff:*),Read,Glob,Grep,Write"

      - name: Post docs consistency review
        if: always()
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PR_NUMBER: ${{ env.PR_NUMBER }}
          REPO: ${{ github.repository }}
          MARKER: "<!-- docs-consistency-check -->"
        run: |
          if [ ! -s "$REVIEW_BODY_FILE" ]; then
            echo "No review body was produced; nothing to post."
            exit 0
          fi

          # --- Static mention guard (deterministic, not AI-dependent) ----------
          # Insert a zero-width space (U+200B) right after EVERY '@' so the text
          # renders identically but GitHub never parses it as a mention. We do
          # not try to match only "real" handles: ZWSP is invisible, so
          # over-inserting is harmless, while missing one would notify someone.
          # Node is guaranteed present (the Claude action runs on it).
          node -e '
            const fs = require("fs");
            const file = process.argv[1];
            let text = fs.readFileSync(file, "utf8");
            text = text.replace(/@/g, "@\u200b");
            fs.writeFileSync(file, text);
          ' "$REVIEW_BODY_FILE"

          # Guarantee the upsert marker is present even if the model omitted it.
          if ! grep -qF "$MARKER" "$REVIEW_BODY_FILE"; then
            printf '\n\n%s\n' "$MARKER" >> "$REVIEW_BODY_FILE"
          fi

          # --- Upsert the review (event=COMMENT so it never blocks merge) ------
          REVIEW_ID=$(gh api --paginate "repos/$REPO/pulls/$PR_NUMBER/reviews" \
            --jq ".[] | select((.body // \"\") | contains(\"$MARKER\")) | select(.state != \"DISMISSED\") | .id" \
            | tail -n 1)

          if [ -n "$REVIEW_ID" ]; then
            gh api --method PUT "repos/$REPO/pulls/$PR_NUMBER/reviews/$REVIEW_ID" \
              -F body=@"$REVIEW_BODY_FILE"
          else
            gh api --method POST "repos/$REPO/pulls/$PR_NUMBER/reviews" \
              -f event=COMMENT \
              -F body=@"$REVIEW_BODY_FILE"
          fi

      - name: Remove trigger label
        if: always() && github.event.action == 'labeled' && github.event.label.name == 'docs-check'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh pr edit "$PR_NUMBER" --repo "${{ github.repository }}" --remove-label docs-check
