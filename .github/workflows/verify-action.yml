name: Verify external action resolution

on:
  workflow_dispatch:

jobs:
  verify:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - uses: tailor-platform/actions/deploy@980aeba08963f4322b2b48ca7a920f4e14876842 # v1.0.0
        with:
          workspace-name: __verify_noop__
          workspace-region: asia-northeast
          organization-id: dummy
          folder-id: dummy
          platform-client-id: dummy
          platform-client-secret: dummy
